# avventura-testuale

Simple CLI game written in C++


What you need to compile
 - CMake
 - make
 - g++

How to compile
 1. Go to the root directory
 2. Type `cmake .`
 3. Type `cmake --build .`
 4. `./avventura`